// import { useAuth } from 'hooks';

export const UserMenu = () => {
  //   const { isLoggedIn } = useAuth();

  return (
    <div>
      <p>mango@mail.com</p>
      <button>Logout</button>
    </div>
  );
};
